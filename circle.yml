machine:
  timezone: Europe/Berlin

  environment:

  node:
    version: 6.9.5

  services:
    - docker

dependencies:
  pre:
    - npm install -g roboter-cli

test:
  override:
    # Do not run integration tests here, since this would require downloading
    # all wolkenkit Docker images to CircleCI, which takes way too long.
    - bot analyse && bot test-units && bot test-e2e
